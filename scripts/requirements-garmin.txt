# Garmin Connect 0.3.10 has not been published to PyPI. Pin the reviewed upstream
# release commit so the bundled runtime uses the current China SSO/MFA protocol.
# It owns token persistence through `Garmin.client.dump()` and deliberately has no
# `garth` dependency. Do not replace this with a floating branch reference.
garminconnect @ git+https://github.com/cyberjunky/python-garminconnect.git@414b54023a31259232744bb67f00a2aa71065e09
